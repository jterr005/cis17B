#include <iostream>
#include <string>
#include "GameMap.h"
using namespace std;

int main() {
	GameMap testMap;
	// if(testMap.pathFind(solution, ))

	/* Test Graph with Outputs */
	testMap.printMap();

	return 0;
}